# Handoff — async-abc paper, 2026-07-30

Paste the prompt at the bottom into a fresh session.

---

## State

- Branch `campaign-tooling`, **~20 commits unpushed** (13 from 2026-07-29 + this
  session's), working tree clean.
- Paper `latex/sn-article-template/sn-article.tex` — **38 pp, compiles clean**
  (0 undefined refs, 0 overfull).
- Every claim in the paper is backed by completed data.
- Venv: `nastjapy_copy/.venv/bin/python`.

## Landed this session

| item | commits |
|---|---|
| Records-free scaling finalize (k-frontier needed **no** re-run) | `7a7cf80` |
| CPM twin failure diagnosis + `--step-timeout-min` + 5 reps | `dd47186` |
| twinB → barrier cost is granularity-independent under a straggler | `c7e7929` |
| k/calibration frontier: **no** trade-off (tab:k-frontier) | `455394b` |
| Stale-summary repair tool (`--summaries-only`) | `c5a2a10` |
| Conservative twin granularity in headline claims | `e30bf55` |
| Heterogeneity twin → the barrier's cost is a **variance** cost | `94afc56` |

### The three things worth knowing

1. **A TIMEOUT is not necessarily lost data.** All 32 k-frontier (k,W) combos ×
   5 reps had *finished* inside the 180 s wall; only the trailing
   `--finalize-only` timed out, because `rebuild_scaling_outputs` loaded every
   `raw_results_w*_k*.csv` shard (~150M records / >10 GB) and all six jobs raced
   to do it. `--finalize-skip-records` restored the aggregates in full at **zero
   node-hours**, instead of the full grid re-run the previous handoff proposed.

2. **Limitation (vii) went from three limits to one.** twinB (barrier every 112
   ≈ the baseline's population of 100) showed the barrier's straggler cost is
   granularity-independent (5×/10×/20× agree to 3 s.f. with the fine twin), and
   the heterogeneity twin closed the second limit. Only the CPM-scale twin
   remains, and it is nearly done (see below).

3. **Two paper claims turned out to be wrong and are now corrected.** The
   archive size does *not* trade calibration against throughput (k=100 dominates
   every larger archive on both axes at once), and the cross-algorithm
   comparison *understates* rather than overstates the barrier's cost (pyABC
   discards latecomers, so our own twin is 5.3× slower than it at σ=2).

## Outstanding

1. **CPM twin (`cpmtwin_20260729`) — refill to 5 replicates was in flight.**
   Check `scaling_cpm_twin/data/throughput_summary_w{192,384}_k100.csv` for
   5 reps each (w48/w96 already have 5). Then write it up: it closes the last
   limit in Limitation (vii). The result is ready to state —

   | W | async median | twin median | ratio | async CV | twin CV |
   |---|---|---|---|---|---|
   | 48 | 9.33 | 4.87 | 1.9× | 0.9% | 50.7% |
   | 96 | 18.67 | 10.00 | 1.9× | 1.3% | 53.8% |
   | 192 | 37.75 | (15.11, 2 reps) | 2.5× | 0.6% | — |
   | 384 | 71.98 | (16.52, 3 reps) | 4.4× | 1.3% | 75.1% |

   Two findings: the twin ratio (1.9→4.4×) **matches the pyABC baseline ratio**
   (2.4→5×) at CPM scale, unlike the straggler case where the twin was far
   slower than pyABC — so the barrier is the dominant cost there and the
   cross-algorithm comparison is not inflated by non-barrier differences. And
   the barrier makes throughput **unpredictable**: 50–75% CV across seeds versus
   <1.5% for the async arm, because every generation is gated by the most
   expensive parameter draw in it. Async CPM source is
   **`rerun_20260707/scaling_cpm`** (9.33 at W=48), not the older run.

2. **Two `make_*_fig.py` scripts have stale `--refresh` paths** —
   `make_lv_scaling_fig.py` (points at `run_full_20260626_1816`, which gives
   595 sims/s at k=100/W=48 against the paper's 6240) and
   `make_cpm_scaling_fig.py`. Both produce figures the paper **no longer
   includes** (superseded by `fig_scaling_combined`, which is correctly
   vendored), so the paper is unaffected — but anyone regenerating would get
   silently wrong numbers. Either repoint them at `rerun_20260707` or delete
   them as superseded.

3. **`scaling` / `scaling_cpm` summary quality columns** — the regen job
   (`14161248`, mem192, `--summaries-only`) was still running on these. The
   straggler one came back a **no-op** (see the correction below), so these two
   may well be no-ops too; check the job log for the reported `|change|`.

## Deferred (pre-existing)

- Theorem → Proposition + explicit CLT rate (concern 1, venue-dependent).
- Placeholder front matter: authors, funding, data/code repository links.

## Correction to the previous handoff's item 5

The previous handoff said `runtime_summary.final_quality_wasserstein` for
**straggler/hetero** was pre-kernel-fix on disk. Both parts were wrong:

- **hetero** emits no such column at all, and its `gaussian_analytic_summary`
  goes through `_weighted_final_frame`, which never touches the
  kernel-dependent archive reconstruction.
- **straggler**'s column was already correct. Recomputing it with the run's own
  kernel reproduces the on-disk values **exactly** (median and max |change| =
  0.0000 over all 50 rows). The apparent discrepancy that made it look stale
  was a schema/strategy mismatch on my side: `quality_vs_wall_time_data.csv`
  uses `time_uniform` checkpoints while the summary uses `quantile`. Compared
  like-for-like against the *diagnostic* (quantile) curve, disk, pre-fix and
  post-fix values all agree to four decimals.

The `--summaries-only` tool added in `c5a2a10` is still worth having (and is
tested), but treat it as a verification pass, not a known repair. **Before
"fixing" a stale artifact, check that the two things you are comparing use the
same checkpoint strategy.**

## Hard-won gotchas — do not rediscover

- **On a TIMEOUT/FAILED job, check whether the simulation work finished** before
  concluding data loss. `scaling_cpm_single.sh` marks the whole job FAILED if
  *any* per-combo srun fails, and the k-frontier's TIMEOUTs were all in the
  finalize.
- **The barrierized twin intermittently deadlocks at ≥192 ranks** (2 of 6 CPM
  replicates; one wedged in teardown with "191 pending intra-island send
  requests", one after 2 evaluations). It is intermittent — the w384 replicate
  that hung completed on retry. Cap the step (`--step-timeout-min`) so a hang
  costs 60 min on 8 nodes, not 120.
- **Only the barrier arm needs sim-limiting.** A collective barrier needs
  identical per-rank call counts; wall-clock stops ranks independently and
  deadlocks. Async runs wall-limited. The two are compared as **rates**
  (sims / `active_wall_time_s`), never as a matched budget.
- **The twin never has to fit the async arm's wall budget** — this is what made
  the previous handoff think σ=2 heterogeneity was infeasible. It is a ~1200 s
  per-replicate run.
- **Size per-cell budgets from measured rates.** The heterogeneity rate spans
  117× from σ=0 to σ=2; one monolithic `max_simulations` cannot serve both.
- **Never give both arms the same budget** (per-worker budgets make both arms
  straggler-gated and erase the effect).
- **Verify which run a scratch path belongs to.** The paper's LV/CPM scaling
  data is `rerun_20260707`; several figure scripts still point at pre-rerun runs.
- `sbc_`-prefixed experiments resolve by convention; every other new experiment
  needs entries in **all three** registries (`run_all_paper_experiments`,
  `shard_finalizers`, `sharding`) — plus watch the hidden allowlists
  (`schema.VALID_BENCHMARK_NAMES`, `submit_replicate_shards`'s own check).
- Summary CSVs name the tagged run **`method`** (straggler/hetero) or
  **`method_variant`** (scaling) — a repair pass keyed on one silently skips
  the other.
- `submit_scaling_cpm.py` takes `output_dir` **positionally**; its walltime
  estimator returns 0 s for sim-limited runs → pass `--min-time`.
- rsync deploys must use **full destination paths**.

---

## Prompt for the next session

Continue the async-abc paper campaign. Read first, in order:

1. Auto-memory `project_full_review_rerun_campaign.md` — the BOTTOM entries
   (the 2026-07-30 block has this session's state, results and gotchas).
2. `.plans/HANDOFF_2026-07-30.md` — this file, including the "Correction to the
   previous handoff's item 5" section.
3. `git log --oneline -20` and `git status` (branch `campaign-tooling`,
   ~20 unpushed commits, clean tree).

Context: the paper is at 38 pp and compiles clean; every claim in it is backed
by completed data. Limitation (vii) has one limit left (no CPM-scale twin) and
the run that closes it was in flight. Venv is `nastjapy_copy/.venv/bin/python`.
Commit as you go on `campaign-tooling`, do NOT push, omit Co-Authored-By lines.

Work the "Outstanding" list in order:

1. Confirm the CPM twin refill reached 5 replicates at w192/w384, then write it
   into §results-hpc and close the last limit in Limitation (vii). The numbers
   and the two findings (ratio matches pyABC at CPM scale; barrier makes
   throughput 50–75% CV unpredictable vs <1.5% async) are in the table above.
2. Repoint or delete the two stale-path figure scripts.
3. Check the regen job's reported |change| for `scaling` / `scaling_cpm`.

Before spending cluster time, re-read "Hard-won gotchas" — this session
recovered a full grid's worth of node-hours by checking whether a TIMEOUT had
actually lost any data, and the previous session burned ~16 node-hours on a
single un-capped twin hang.
