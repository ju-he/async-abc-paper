# Handoff — async-abc paper, 2026-07-30

Paste the prompt at the bottom into a fresh session.

---

## State

- Branch `campaign-tooling`, **everything unpushed** (25 commits from this
  session; check with `git log --oneline campaign-tooling ^main | wc -l`),
  working tree clean.
- Paper `latex/sn-article-template/sn-article.tex` — **39 pp, compiles clean**
  (0 undefined refs, 0 overfull).
- Every claim in the paper is backed by completed data. **No campaign jobs are
  queued or running** — all of this campaign's jobs are named `abc_*`, so filter
  with `squeue --me -h -o "%j" | grep '^abc_'`; unrelated workloads of the
  user's may share the account (a `V2cov` array was running at handoff time).
- Test suite: **707 passed, 10 skipped**.
- Venv: **`.venv/bin/python` at the repo root** — rebuilt this session, see below.

## Landed this session

| item | commits |
|---|---|
| Records-free scaling finalize (k-frontier needed **no** re-run) | `7a7cf80` |
| CPM twin failure diagnosis + `--step-timeout-min` + 5 reps | `dd47186` |
| twinB → barrier cost is granularity-independent under a straggler | `c7e7929` |
| k/calibration frontier: **no** trade-off (tab:k-frontier) | `455394b` |
| Stale-summary repair tool (`--summaries-only`) | `c5a2a10` |
| Conservative twin granularity in headline claims | `e30bf55` |
| Heterogeneity twin → the barrier's cost is a **variance** cost | `94afc56` |
| Per-producer checkpoint strategy + no NaN-filling in the repair pass | `ff8088d` |
| Repointed the two superseded scaling figure scripts | `e587102` |
| CPM-scale twin → **Limitation (vii) fully closed** | `caf49a5` |
| Vendored the data behind all three twin tables | `aea8903` |
| Corrected my own 0.115 attribution | `ee0a91e` |
| CPM twin retry wrapper; faulthandler made opt-in | `1ae033f`, `12c9d50`, `1842521` |
| **CPM twin at 5 replicates — attribution weakened** | `c1a9751` |
| Rebuilt the venv at `.venv`; repointed every stale reference | `1c4edfd` |

### The three things worth knowing

1. **A TIMEOUT is not necessarily lost data.** All 32 k-frontier (k,W) combos ×
   5 reps had *finished* inside the 180 s wall; only the trailing
   `--finalize-only` timed out, because `rebuild_scaling_outputs` loaded every
   `raw_results_w*_k*.csv` shard (~150M records / >10 GB) and all six jobs raced
   to do it. `--finalize-skip-records` restored the aggregates in full at **zero
   node-hours**, instead of the full grid re-run the previous handoff proposed.

2. **Limitation (vii) is now fully closed** — it had three open limits at the
   start of the session. twinB (barrier every 112 ≈ the baseline's population of
   100) showed the barrier's straggler cost is granularity-independent
   (5×/10×/20× agree to 3 s.f. with the fine twin); the heterogeneity twin closed
   the second; the CPM-scale twin closed the third. The twin now isolates
   synchronization on **all three** benchmarks where systems gains are reported:
   21–402× (straggler), 1.1–25× (heterogeneity), 1.9–2.8× (Cellular Potts). The
   attribution's *strength* differs by benchmark, and the paper says so: near-total
   under a straggler or heterogeneity, **partial** on Cellular Potts (see the
   retraction section below). What remains is scoped as a limitation of the twin
   as an *instrument* (it deadlocks at ≥192 ranks), not as an attribution gap.

   The CPM result's second finding is the more interesting one: the barrier makes
   throughput **unpredictable**, not just lower — 25–54% CV across seeds against
   the asynchronous arm's 0.6–1.3%, on identical proposals and identical seeds,
   because a generation inherits the cost of its most expensive parameter draw.

3. **Two paper claims turned out to be wrong and are now corrected.** The
   archive size does *not* trade calibration against throughput (k=100 dominates
   every larger archive on both axes at once), and the cross-algorithm
   comparison *understates* rather than overstates the barrier's cost (pyABC
   discards latecomers, so our own twin is 5.3× slower than it at σ=2).

## Outstanding

**Nothing is blocked.** Cluster queue empty; every claim backed by completed data.

1. **The venv moved to `.venv` (repo root).** The old `nastjapy_copy/.venv` was a
   symlink into `Code/mirrors/`, and that whole directory vanished from the sync
   share mid-session — not caused by anything here (the rsync is a push with no
   `--delete` and excludes `nastjapy_copy`; git does not remove ignored paths).
   Rebuilt on the user's instruction and at full parity (707 passed, 10 skipped);
   `CLAUDE.md`, `AGENTS.md`, `README.md`, `cellular_potts.py` and three test skip
   messages were all repointed. **The suite has been re-run, so the commits from
   `c5a2a10` onward are verified.** Rebuild recipe is in `CLAUDE.md`; note
   propulate and nastjapy come from the sibling checkouts and `uv sync` would
   wrongly take propulate from PyPI.

2. **Optional, not requested:** 8 mpirun-dependent tests silently skip in every
   full-suite run, because `from mpi4py import MPI` initializes MPI in the pytest
   process and `mpirun` then refuses to launch. They pass when
   `test_mpi_hardening.py` runs alone. Fix: probe once at session start, before
   anything imports MPI, and cache it in a session-scoped fixture.

3. **Deferred (pre-existing):** Theorem → Proposition + explicit CLT rate
   (concern 1, venue-dependent); placeholder front matter (authors, funding,
   data/code repository links).

## The CPM twin conclusion changed when the last replicates landed — read this

At three and four replicates the twin ratios were 1.9/1.9/2.1/4.4×, which
tracked pyABC's 2.4/4.1/4.8/5.1× closely enough that I wrote "the barrier
accounts for essentially the whole cross-algorithm gap". **That is retracted.**
With five replicates everywhere the twin is 1.9/1.9/1.9/2.8× — nearly flat to
four nodes — so it is consistently the *faster* synchronous arm and the distance
from pyABC *widens* with scale. The barrier is now stated as the largest single
identified component of the CPM gap (roughly half to three-quarters in log
terms), with the growing residual attributed to pyABC's other differences.

Lesson: two missing replicates out of five were enough to invert a qualitative
claim, because the missing ones were the fast ones. Do not write an attribution
sentence off a partial replicate set.

The variance finding is unaffected and is the stronger result: twin CV 25–54%
against the asynchronous arm's 0.6–1.3% on identical seeds.

## The faulthandler is not a free diagnostic

I enabled `SCALING_FAULTHANDLER_S` on a retry as "insurance". It produced 9
segfaults at 384 ranks and 2 at 192, against **zero** across all four earlier
runs of the same combos (which failed by hanging). It converts hangs into
crashes, so a run using it is not measuring the same thing as one without it.
Now opt-in in `scaling_cpm_twin_retry.sh`, with a warning that its throughput
must be discarded. The clean retry that followed landed the missing replicate in
20 minutes.

## Corrections to the previous handoff's item 5 — and to my own first attempt

The previous handoff said `runtime_summary.final_quality_wasserstein` for
**straggler/hetero** was pre-kernel-fix on disk. Both parts were wrong, but
**two other experiments genuinely were stale**:

- **hetero** emits no such column at all; its `gaussian_analytic_summary` goes
  through `_weighted_final_frame`, which never touches the kernel-dependent
  archive reconstruction.
- **straggler** was already correct — recomputation reproduced it to 1.1e-16
  (CSV round-trip noise). There the hard and top-k archive rules coincide at the
  final checkpoint. What made it *look* stale was my comparing the summary
  against `quality_vs_wall_time_data.csv`, which uses `time_uniform` checkpoints
  and cross-replicate aggregation, while the summary uses `quantile`.
- **`scaling` and `scaling_cpm` WERE stale**, and are now repaired (job
  `14161251`). The signature confirms the cause: only `async_propulate_abc` rows
  moved — 25 of 146 on `scaling_cpm`, 28 of 180 on `scaling`, zero
  `abc_smc_baseline` rows — exactly as the kernel bug predicts, since it acts on
  `_async_archive_rows` while the sync baseline uses generation snapshots. Max
  shifts 0.1149 and 0.0943 against a Wasserstein scale of ~0.4–0.5. No paper
  claim moves: the only vendored artifact carrying this column is `tab_twin`'s,
  which comes from the post-fix twin runs.

I also got the *mechanism* wrong mid-way and corrected it in `ee0a91e`. I blamed
the 0.115 on a checkpoint-strategy mismatch in my own repair pass. It is not:
`quantile` (8 checkpoints) and `all` (500) both return 0.520199 on
`scaling_cpm` w48/k100/async/rep0, because both place their last checkpoint at
the maximum `axis_value` and this metric *is* the last checkpoint's value. The
0.115 is a real kernel correction. The pass still selects the strategy per
producer (correct, and robust if the metric ever stops being "the final
checkpoint"), and the *other* bug that commit fixed was real and did matter: the
pass had been filling deferred NaN placeholders, which would have made a later
finalize skip its own backfill.

**Two rules out of this:** before "fixing" a stale artifact, check that both
sides use the same checkpoint strategy *and* the same aggregation; and before
attributing a difference to a strategy, verify it actually moves the number.

## Hard-won gotchas — do not rediscover

- **On a TIMEOUT/FAILED job, check whether the simulation work finished** before
  concluding data loss. `scaling_cpm_single.sh` marks the whole job FAILED if
  *any* per-combo srun fails, and the k-frontier's TIMEOUTs were all in the
  finalize.
- **The barrierized twin intermittently deadlocks at ≥192 ranks** (2 of 6 CPM
  replicates; one wedged in teardown with "191 pending intra-island send
  requests", one after 2 evaluations). It is intermittent — the w384 replicate
  that hung completed on retry. Cap the step (`--step-timeout-min`) so a hang
  costs 60 min on 8 nodes, not 120.
- **Only the barrier arm needs sim-limiting.** A collective barrier needs
  identical per-rank call counts; wall-clock stops ranks independently and
  deadlocks. Async runs wall-limited. The two are compared as **rates**
  (sims / `active_wall_time_s`), never as a matched budget.
- **The twin never has to fit the async arm's wall budget** — this is what made
  the previous handoff think σ=2 heterogeneity was infeasible. It is a ~1200 s
  per-replicate run.
- **Size per-cell budgets from measured rates.** The heterogeneity rate spans
  117× from σ=0 to σ=2; one monolithic `max_simulations` cannot serve both.
- **Never give both arms the same budget** (per-worker budgets make both arms
  straggler-gated and erase the effect).
- **Verify which run a scratch path belongs to.** The paper's LV/CPM scaling
  data is `rerun_20260707`; several figure scripts still point at pre-rerun runs.
- `sbc_`-prefixed experiments resolve by convention; every other new experiment
  needs entries in **all three** registries (`run_all_paper_experiments`,
  `shard_finalizers`, `sharding`) — plus watch the hidden allowlists
  (`schema.VALID_BENCHMARK_NAMES`, `submit_replicate_shards`'s own check).
- Summary CSVs name the tagged run **`method`** (straggler/hetero) or
  **`method_variant`** (scaling) — a repair pass keyed on one silently skips
  the other. That naming also tells you which checkpoint strategy produced the
  row: `method` → `quantile`/8 (`runtime_summary`), `method_variant` → `all`
  (`scaling_runner`).
- **NaN in a scaling per-combination shard is a deferred marker, not a missing
  value.** The runners defer the quality metric to `_backfill_quality_metrics`
  at finalize; anything that fills those NaNs makes the next finalize skip its
  own backfill.
- `submit_scaling_cpm.py` takes `output_dir` **positionally**; its walltime
  estimator returns 0 s for sim-limited runs → pass `--min-time`.
- rsync deploys must use **full destination paths**.

---

## Prompt for the next session

Continue the async-abc paper campaign. Read first, in order:

1. Auto-memory `project_full_review_rerun_campaign.md` — the BOTTOM entries
   (the 2026-07-30 block has this session's state, results and gotchas).
2. `.plans/HANDOFF_2026-07-30.md` — this file. Do not skip the two retraction
   sections ("The CPM twin conclusion changed…" and "Corrections to the previous
   handoff's item 5…"); both record claims that were wrong and are now fixed.
3. `git log --oneline -25` and `git status` — expect branch `campaign-tooling`,
   a clean tree, and everything unpushed
   (`git log --oneline campaign-tooling ^main | wc -l`).

Context: the paper is at 39 pp, compiles clean, and every claim in it is backed
by completed data. Limitation (vii) is closed on all three benchmarks where
systems gains are reported. No campaign jobs are pending (they are all named
`abc_*`; other workloads may share the account) and the test suite passes
(707 / 10 skipped). Venv is `.venv/bin/python` at the repo root.
Commit as you go on `campaign-tooling`, do NOT push, omit Co-Authored-By lines.

**There is no outstanding campaign work.** Everything the 2026-07-29 handoff
listed is done, and so is everything this session added. What is left is the
user's call, not a queue to work through:

- **Publication decisions (pre-existing):** Theorem → Proposition with an
  explicit CLT rate (venue-dependent), and the placeholder front matter —
  authors, funding, data/code repository links.
- **Optional, never requested:** make the 8 mpirun-dependent tests actually run
  in a full-suite invocation (see Outstanding item 2).
- **Optional:** push the branch. Every commit on `campaign-tooling` has
  deliberately been kept local across both sessions.

So start by asking the user what they want, rather than inventing work. If they
ask for more cluster runs, re-read "Hard-won gotchas" first — this session
recovered a full grid's worth of node-hours by checking whether a TIMEOUT had
actually lost any data, and lost a job to a diagnostic that changed the failure
mode it was meant to observe.
