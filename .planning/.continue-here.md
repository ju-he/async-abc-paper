---
context: default
status: paused
last_updated: 2026-06-28
branches: async-abc-paper refactor/general@9423cc9 ; propulate feature/async-abc@d9ab64f (pushed to cluster)
---

# Continue here — async-ABC production runs, OOM fixes, paper draft (ad-hoc, NOT a GSD phase)

## Critical Anti-Patterns (do NOT repeat)
| Pattern | What happened | Severity | Prevention |
|---|---|---|---|
| Monitoring the wrong progress metric on a sharded run | I counted ALL `*.csv` under a sensitivity shard as "configs done" (99→141 "climbing", reported healthy). Those were TRANSIENT per-config propulate attempt-log CSVs. Real finalized results are `sensitivity/data/sensitivity_*.csv` — shard-0 had only **3 of 192** while siblings had 192. My "~144/193, half-pace" was wrong; shard-0 was effectively stalled. | blocking | For sharded runs, count FINALIZED variant CSVs in `<shard>/sensitivity/data/sensitivity_*.csv`, NOT all `*.csv`. Compare to a sibling shard. |
| Treating an OOM as single-cause | gaussian_mean had TWO OOM causes. Fixed `extract_posterior` (validated async-only 2GB), but the FULL multi-method run still OOM'd at 49GB (second cause in `plot_benchmark_diagnostics`). The async-only profiler missed it. | advisory | Validate a memory fix by reproducing the FULL config (all methods/reps), not a reduced one. |
| Over-cautious cancel on a misread | I cancelled the sensitivity backfill fearing data loss (CSV 141→3); it was NOT data loss (transient logs cleared on --extend resume; 3 finalized + 7 checkpoints intact). | advisory | Distinguish finalized-result files from transient logs before alarming. |

<current_state>
JUWELS production sweep COMPLETE except sensitivity shard-0 (dropped by user decision). Paper first draft
written and compiling. Two OOM fixes committed; one (async-abc plotting) NOT yet deployed to cluster. No
cron/monitor running. Paused at near-exhausted context.
</current_state>

<session_2026_06_28_resume>
RESUMED 2026-06-28 (driving both remaining items to done):
- DEPLOY DONE: async-abc 9423cc9 rsync'd to cluster. Deploy mechanism FOUND (no push alias; plain rsync —
  see memory/reference_asyncabc_cluster_deploy.md). Blocker resolved.
- SENSITIVITY DONE: finalize job 14066118 COMPLETED. Produced top-level run_full_20260626_1816/sensitivity/
  (192 variant CSVs, 3 reps each from shards 1-3; heatmap pdf/png). Did NOT falsify shard status — ran
  finalize_experiment_by_name directly over the existing shards (shard-0 left state=running). Paper panel
  WIRED: figures/fig_sensitivity_heatmap.pdf + \ref{fig:sensitivity} replaced the \todo at sn-article.tex
  §Sensitivity.
- GAUSSIAN DONE: re-run job 14066112 COMPLETED (exit 0:0, MaxRSS 28.8 GB — was 49–54 GB OOM), 16 plots,
  output /p/scratch/tissuetwin/herold2/async-abc/gaussian_rerun_20260628/gaussian_mean/. async recovers the
  analytic posterior mean to 0.002–0.013 abs err (≈ sync baseline, ≪ rejection). OOM closed (both causes).
- GAUSSIAN PANEL WIRED: figures/fig_gaussian_recovery.pdf (= quality_vs_wall_time.pdf) + \ref{fig:gaussian-recovery}
  replaced the \todo at §Posterior recovery; abstract "two figure panels … being regenerated" clause removed.
- PAPER COMPILES CLEAN: latexmk rc=0, 0 undefined refs. Fig 3 (gaussian, p9) + Fig 9 (sensitivity, p13).
  PDF is 32 MB (too big to send raw — prior workflow used a compressed ~2.5 MB variant).

ALL RESUME WORK COMPLETE. Both \todo data panels filled. Remaining open \todo's in the paper are author-only
(acknowledgements, funding, data-availability, code-availability, author-contributions) — out of scope for the
agent. Working tree NOT committed (commit only on request). Next natural step: author polish + commit when ready.
</session_2026_06_28_resume>

<completed_work>
1. Monitored full sweep to completion (lotka scaling 1/4/16/48/128/256, realistic workload scaling, gaussian/gandk/lotka/
   realistic_workload/sbc/straggler/runtime_het/ablation/sensitivity_gandk). Cancelled straggler_000 (teardown
   hang; work already complete+merged — zero data loss).
2. gaussian_mean OOM (54GB) = TWO causes, both fixed:
   - extract_posterior O(n*k) over full history -> CHUNKED. propulate `feature/async-abc@d9ab64f`, PUSHED to
     cluster via `pushpropulate`. Validated: 1-rep async-only repro MaxRSS 1.97GB (was 53.96).
   - plot_benchmark_diagnostics held full multi-method history (49GB) -> `_subsample_history_for_plots`
     (keeps exact top-k by loss; thins dense curves; cfg `plots.max_history_records_for_plots`, default 200k).
     async-abc `refactor/general@9423cc9`. **NOT yet deployed to cluster.** Unit-verified locally.
3. Paper first draft: `latex/sn-article-template/sn-article.tex` (15 pp, compiles, 0 undefined refs). 17 real
   figures in `figures/`, 11 bib entries added. Method/theory from concept (smooth-kernel + streaming AMIS,
   consistency+CLT). Real results: SBC table, scaling table, straggler numbers. Two `\todo` panels: gaussian
   recovery-vs-time, sensitivity grid. Compressed PDF (2.5MB) delivered (32MB original failed on size).
4. Sensitivity: shard-0 stalled (7 configs in 24h — intermittent pscom per-config MPI teardown hang; non-
   scaling jobs lack PROPULATE_SKIP_DISCONNECT). User decided DROP shard-0. Backfill 14064734 cancelled.
   Prep-doc STATUS = RESOLVED_DROP_SHARD0.
</completed_work>

<remaining_work>
1. Deploy async-abc `9423cc9` to the cluster (deploy mechanism UNKNOWN — `pushpropulate` only covers the
   propulate fork; find/ask for the async-abc rsync/push), THEN re-run gaussian_mean via the jsc-mpc MCP
   (both OOM fixes in place) -> fills the paper's gaussian validity `\todo`. Re-run ~ the gaussian_full job
   14063810 command, fixes deployed.
2. Run sensitivity finalize/merge over shards 1-3 (+ shard-0's 3 partial variants) -> top-level
   `run_full_20260626_1816/sensitivity/` + plots -> wire that panel into the paper.
3. If re-running any non-scaling multi-rank job: set PROPULATE_SKIP_DISCONNECT=1 to avoid the teardown hang.
</remaining_work>

<blockers>
- Deploy command for the async-abc repo -> cluster is unknown (pushpropulate is propulate-only). Needed before the gaussian re-run picks up the plotting fix.
</blockers>

## Required Reading (in order)
1. `.plans/sensitivity_backfill_ready.md` — live status (gaussian fix detail + sensitivity RESOLVED_DROP_SHARD0 + backfill artifacts).
2. `.plans/bug-fixes/previous-fixes.md` — 2026-06-27 gaussian OOM entry.
3. `latex/sn-article-template/sn-article.tex` — the draft (two `\todo` panels mark open data).
4. `memory/project_scaling_mpi_fixes.md` — jsc-mpc MCP playbook + mount path.

## Infrastructure State
- jsc-mpc MCP: juwels-cluster, project `async-abc-paper` (soft 24 / hard 96 nh). Mount
  `/home/juhe/remotes/scratch/herold2/async-abc` <-> cluster `/p/scratch/tissuetwin/herold2/async-abc`.
- NO cron/monitor running (stopped at pause). Nothing auto-firing.
- propulate fork deployed to cluster (d9ab64f); async-abc 9423cc9 NOT deployed.

<next_action>
Find the async-abc -> cluster deploy command (ask user / look for an alias like `pushpropulate`), deploy
`9423cc9`, then re-run gaussian_mean via the jsc-mpc MCP to regenerate its plots and fill the paper panel.
</next_action>
