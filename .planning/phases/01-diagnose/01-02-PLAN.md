---
phase: 01-diagnose
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .plans/diagnose/mpi-evaluation.md
autonomous: false
requirements:
  - MPI-02
  - MPI-04
must_haves:
  truths:
    - "File .plans/diagnose/mpi-evaluation.md contains a characterization table with one row per candidate and columns: cluster stability, correctness vs standard pyABC, effect on paper results, residual risk"
    - "File contains a paper sensitivity assessment section addressing the four questions from D-07 (particle correctness, wall-time semantics, sampler overhead, paper conclusion sensitivity)"
    - "File contains a residual risks + newly discovered hang paths section with at least one reproduction recipe (worker crash during bcast)"
    - "File contains a Recommendation section with a single clear winner (not hedged) and evidence-based rationale"
    - "A human has reviewed the complete document and confirmed all four candidates are characterized and the recommendation is clear"
  artifacts:
    - path: ".plans/diagnose/mpi-evaluation.md"
      provides: "Complete Phase 1 diagnose deliverable: inventory + characterization + recommendation"
      contains: "## Recommendation"
      min_lines: 350
  key_links:
    - from: ".plans/diagnose/mpi-evaluation.md §Recommendation"
      to: ".plans/bug-fixes/previous-fixes.md"
      via: "evidence citations from bug history"
      pattern: "Apr [4-8]"
    - from: ".plans/diagnose/mpi-evaluation.md §Characterization Table"
      to: ".plans/diagnose/mpi-evaluation.md §Candidate [1-4]"
      via: "each table row summarizes the candidate section above"
      pattern: "CommWorldMap.*Shared MPICommExecutor.*ConcurrentFuture"
---

<objective>
Append the characterization table, paper sensitivity assessment, residual risks / newly discovered hang paths, and final recommendation sections to `.plans/diagnose/mpi-evaluation.md`. Then pause for human verification that the document is complete, the evaluation is fair, and the recommendation is clearly justified.

Purpose: This completes the Phase 1 deliverable. The document then serves as Phase 2's implementation target. Addresses MPI-02 (full evaluation with rationale, paper sensitivity) and closes MPI-04 (residual hang paths documented with reproduction recipes).

Output: Complete `.plans/diagnose/mpi-evaluation.md` ready to hand to Phase 2 planning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-diagnose/01-CONTEXT.md
@.planning/phases/01-diagnose/01-RESEARCH.md
@.planning/phases/01-diagnose/01-01-SUMMARY.md
@.plans/diagnose/mpi-evaluation.md
@.plans/bug-fixes/previous-fixes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Characterization Table and Paper Sensitivity Assessment and Residual Risks sections</name>
  <files>.plans/diagnose/mpi-evaluation.md</files>
  <read_first>
    - .plans/diagnose/mpi-evaluation.md (existing per-candidate sections from Plan 01 — append, do not overwrite)
    - .planning/phases/01-diagnose/01-RESEARCH.md sections "Paper Sensitivity Assessment (D-07)", "Open Questions" (all three), and "Common Pitfalls" (reproduction recipes for newly discovered hang paths)
    - .planning/phases/01-diagnose/01-CONTEXT.md (D-07 four questions verbatim: particle correctness, wall-time semantics, sampler overhead, paper conclusion sensitivity)
    - .planning/phases/01-diagnose/01-01-SUMMARY.md (any corrections Plan 01 noted about line numbers or content structure)
    - .plans/bug-fixes/previous-fixes.md (for any evidence citations not already made in Plan 01)
  </read_first>
  <action>
APPEND (do not rewrite) three new top-level sections to the end of `.plans/diagnose/mpi-evaluation.md`. Use the Write or Edit tool to preserve all existing content from Plan 01.

The three sections to append, in order, are:

**Section A: Characterization Table**

Start with a `---` horizontal rule, then the heading `## Characterization Table`. Produce a markdown table with these exact column headers (in order): `Candidate`, `Cluster Stability`, `Correctness vs Standard pyABC`, `Effect on Paper Results`, `Residual Risk`. The table must have exactly 4 data rows, one per candidate. Row keys must start with `1. CommWorldMap`, `2. MappingSampler + Shared MPICommExecutor`, `3. ConcurrentFutureSampler + Per-call MPICommExecutor`, `4. MappingSampler + Per-call MPICommExecutor.map`.

Row 1 (CommWorldMap):
- Cluster Stability: "Verified at 16 ranks post-Apr 8 for lotka_volterra, straggler, gaussian_mean, gandk, sbc. UNVERIFIED at 48 ranks. No inter-communicators means it eliminates the ParaStation Disconnect hang class entirely."
- Correctness: "MappingSampler(map_=CommWorldMap.map) satisfies pyABC's documented MappingSampler interface — pyABC accepts any map(fn, iter) callable. CommWorldMap preserves input order (results indexed by idx). Particle acceptance semantics identical to standard pyABC."
- Paper Effect: "Negligible. Coordination overhead sub-millisecond per batch at 48 workers and 100-1000 population. No inter-communicator teardown overhead."
- Residual Risk: "(a) Worker crash during bcast causes root hang (theoretical, not observed). (b) 48-rank stability with ParaStation MPI unverified on any current job."

Row 2 (Shared MPICommExecutor):
- Cluster Stability: "Survives at 48 ranks for the scaling workload pattern (one shared executor per n_workers). Verified Apr 7. Depends on structural luck: one Create_intercomm/Disconnect cycle total."
- Correctness: "Uses pyABC MappingSampler with executor.map — matches pyABC documentation exactly. Identical particle acceptance semantics."
- Paper Effect: "~40-50s teardown overhead per n_workers value (one-time). Not significant vs the 900s wall-time budget when amortized across k-values and replicates."
- Residual Risk: "The single Disconnect still deadlocked in other non-scaling experiments (lotka_volterra at 48 ranks, Apr 8 entry). 'Happens to work' due to the server-loop usage pattern, not because Disconnect is reliable. Fragile to ParaStation MPI version changes."

Row 3 (ConcurrentFutureSampler):
- Cluster Stability: "KNOWN BROKEN at 48 ranks on ParaStation MPI. Multiple reproducible hangs documented Apr 5-7. Retained opt-in with warning at pyabc_sampler.py:296-302."
- Correctness: "Uses pyABC documented speculative future interface. Minor semantic deviation: workers may start simulating particles for next tolerance threshold before current generation completes (pre-submission via client_max_jobs)."
- Paper Effect: "Under wall-time budgets this can let slightly more work complete than strict sync semantics allow. Effect bounded by client_max_jobs and small in absolute terms."
- Residual Risk: "Entire path is the known failure mode. No unresolved sub-bugs; the known behavior IS the risk. Useful only for diagnostic comparison."

Row 4 (Per-call Mapping):
- Cluster Stability: "KNOWN BROKEN at 48 ranks on ParaStation MPI. Was default Apr 7 to 8 and hung in the second abc.run() at 48 ranks (Apr 7 entries). Single-cycle hangs also documented Apr 8."
- Correctness: "This IS the standard pyABC MPI usage pattern from pyABC's own documentation. Particle acceptance semantics identical to Candidate 1 and 2 (synchronous blocking map)."
- Paper Effect: "Teardown overhead per abc.run() call, measured 37-50s per call (Apr 7). At many calls, eats a meaningful fraction of the 900s budget."
- Residual Risk: "Per-call Create_intercomm/Disconnect cycles are the documented hang pattern. No path forward without rewriting mpi4py or ParaStation MPI."

After the table, add a paragraph starting with "**Table summary:**" that concludes: Candidates 3 and 4 are eliminated by the bug history; Candidate 2 works only in its specific scaling runner pattern and remains fragile; Candidate 1 is the only candidate that eliminates the inter-communicator hang class by design, pending 48-rank verification (Phase 2 MPI-01 work).

**Section B: Paper Sensitivity Assessment (per D-07)**

Heading `## Paper Sensitivity Assessment (per D-07)`. Opening paragraph notes: "Assessed at reasoning level only — no new mpirun runs or timing numbers (per CONTEXT.md D-05 and D-07)."

Then four subsections with headings `### Q1: Does sampler choice affect which particles get accepted (correctness of ABC-SMC)?`, `### Q2: Does sampler choice change wall-time semantics?`, `### Q3: Is sampler overhead significant relative to the 900s wall-time budget?`, `### Q4: Is the paper's primary claim sensitive to sampler choice?`.

Q1 answer: "No, for all four candidates." Explain that MappingSampler and ConcurrentFutureSampler both pass the same simulate_fn to pyABC's ABCSMC loop; particle selection, epsilon schedule, and weight normalization are handled entirely inside pyabc.ABCSMC; the sampler only controls _how_ simulations are dispatched, not _which_ results are accepted. CommWorldMap's map preserves input order by indexing results with idx (pyabc_sampler.py:115-140), so no reordering. All four candidates are correctness-equivalent.

Q2 answer: "Potentially yes, marginally, for Candidate 3 only." Explain ConcurrentFutureSampler pre-submits up to client_max_jobs futures speculatively, so workers can start on next tolerance threshold before current generation completes. Under wall-time this lets slightly more work complete, but effect is bounded by client_max_jobs (<= n_workers after Apr 5 fix) and small. Candidates 1, 2, 4 all use synchronous blocking map and have identical wall-time semantics.

Q3 answer: Bullet list per candidate. Candidate 1: "NO. bcast + send*k + recv*k per batch; sub-millisecond at 48 workers × 100-1000 population." Candidate 2: "Small. One Create_intercomm/Disconnect cycle per n_workers value, ~40-50s on Apr 7. Amortized across all k-values and replicates." Candidate 3: "Large and hang-prone. Per-call teardown ~37-50s when it succeeds, plus hang risk on subsequent calls." Candidate 4: "Large. Per-call Create_intercomm/Disconnect, ~40-50s per abc.run() on Apr 7. At many calls this eats a meaningful fraction of the 900s budget."

Q4 answer: "No." Explain the paper compares wall-time efficiency of async Propulate-ABC vs sync pyABC. The structural advantage of async (no global synchronization barrier per generation) is independent of which MPI dispatch mechanism sync pyABC uses. Sampler choice only affects sync pyABC's implementation throughput: fragile sampler makes sync pyABC look worse (unwanted bias favoring async), stable sampler represents sync pyABC fairly. CommWorldMap gives sync pyABC its best fair shot. ABC-SMC correctness from Q1 is identical across candidates, so scientific conclusions (accepted particles, posterior shape) are independent of sampler choice; only wall-time fairness depends on it.

**Section C: Residual Risks and Newly Discovered Hang Paths**

Heading `## Residual Risks and Newly Discovered Hang Paths`. Opening paragraph: "Each entry below is either a previously-undocumented residual risk or a hang path identified by this phase's analysis. Per CONTEXT.md D-06, each gets a reproduction recipe or test stub description (not an actual test — test creation is Phase 2's job)."

Then four subsections `### Risk 1: Worker crash during CommWorldMap.map (NEW, not in bug history)`, `### Risk 2: CommWorldMap at 48 ranks — unverified (KNOWN RISK, STATE.md entry)`, `### Risk 3: Pre-Barrier allgather skew (existing fix, regression risk)`, `### Risk 4: pyABC NaN weight on partial generation (existing fix, regression risk)`.

Risk 1 must include: candidate affected is 1 (CommWorldMap); description that if worker crashes/exits/killed during CommWorldMap.map, root's next COMM_WORLD.bcast blocks indefinitely because mpi4py bcast requires all ranks; no timeout, no worker liveness check. Reproduction recipe numbered 1-4: (1) Launch with mpirun -n 3 running a CommWorldMap workload; (2) After at least one generation, send SIGKILL (kill -9) to the highest-rank worker; (3) Expected: root hangs at next bcast in CommWorldMap.map() or CommWorldMap.shutdown(); rank 1 also hangs at next bcast in worker_loop; no process exits; (4) Observation: no further output from surviving processes; py-spy dump on root shows mpi4py.MPI.Comm.bcast as current frame. Mitigation options (Phase 2 scope): (a) MPI_Testsome pattern to poll with timeout (not currently implemented anywhere); (b) catch MPI_ERR_PROC_FAILED from bcast (requires MPI fault tolerance not available in ParaStation MPI default build); (c) document as expected-crash failure mode and rely on Slurm job timeouts as outer safety net.

Risk 2 must include: candidate affected is 1; description that CommWorldMap was developed Apr 8 and verified only on non-scaling jobs at <=16 ranks; scaling runner continues to use Candidate 2; whether COMM_WORLD.bcast/send/recv are stable under ParaStation MPI at 48 ranks is untested. Reproduction recipe (Phase 2 cluster test, not Phase 1): (1) Modify scaling_runner.py to use CommWorldMap-backed sampler instead of shared MPICommExecutor for one test run; (2) Submit scaling_48 job with a small pyABC workload (gaussian_mean, population=100, 3 generations); (3) Expected: job completes without hanging, CSV has non-zero rows; (4) Failure mode: if it hangs, the specific bcast/send/recv call at the hang point identifies the issue. Note "Why this belongs to Phase 2:" D-05 forbids new mpirun/cluster runs in Phase 1; explicitly Phase 2 MPI-01 work.

Risk 3 must include: candidates affected are 1 and 2; description that the COMM_WORLD.Barrier() at pyabc_wrapper.py:413 and abc_smc_baseline.py:399 is NOT cosmetic — it ensures workers have exited worker_loop() before run_method_distributed calls allgather(error_payload); if removed or reordered (thinking it's redundant with allgather), workers entering allgather while root is still in map() will deadlock. For Candidate 2, the equivalent is COMM_WORLD.Barrier() at scaling_runner.py:1067-1068. Test stub description (Phase 2): unit test mocking CommWorldMap.shutdown() to skip the bcast, asserting run_method_distributed deadlocks at allgather (integration-level, observable only via timeout); regression test parsing source for exact Barrier() call sites and failing CI if removed.

Risk 4 must include: all four candidates affected; description that when max_wall_time_s fires mid-generation, pyABC stops collecting particles and the partial population has weight_sum 0 or NaN, causing Population.__init__ to raise AssertionError("The population total weight nan is not normalized"); the fix (Apr 8) catches this specific AssertionError in pyabc_wrapper.py:125-133 and abc_smc_baseline.py:127-136 and falls back to abc.history. Risk: if the fix guard is removed or pyABC error message wording changes, the catch misses. Test stub description (Phase 2): unit test running a pyABC workload with max_wall_time_s=0.1 (force immediate timeout), asserting (a) no exception escapes, (b) returned records list has 0 or more entries but no NaN weights, (c) log contains the "NaN population weight — treating as early wall-time stop" warning.

After appending, verify the document still contains the Plan 01 headings for all four candidates (no content overwritten).

Per CONTEXT.md D-04 (no inline code changes in Phase 1) and D-05 (static analysis only).
  </action>
  <verify>
    <automated>grep -q "## Characterization Table" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "## Paper Sensitivity Assessment" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "## Residual Risks and Newly Discovered Hang Paths" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "Worker crash during CommWorldMap" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "48 ranks" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "NaN" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "Q1:" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "Q4:" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "## Candidate 1: CommWorldMap" .plans/diagnose/mpi-evaluation.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal heading `## Characterization Table`
    - File contains the literal heading `## Paper Sensitivity Assessment (per D-07)`
    - File contains the literal heading `## Residual Risks and Newly Discovered Hang Paths`
    - Characterization table has 4 data rows starting with `1. CommWorldMap`, `2. MappingSampler + Shared MPICommExecutor`, `3. ConcurrentFutureSampler`, `4. MappingSampler + Per-call MPICommExecutor.map`
    - Characterization table has columns `Cluster Stability`, `Correctness vs Standard pyABC`, `Effect on Paper Results`, `Residual Risk` in the header row
    - Paper sensitivity section contains literal strings `Q1:`, `Q2:`, `Q3:`, `Q4:` — all four D-07 questions
    - File contains the literal strings `pyabc_sampler.py:296-302`, `pyabc_wrapper.py:125-133`, `abc_smc_baseline.py:127-136` (fix location citations for warnings and NaN guard)
    - File contains the literal strings `SIGKILL` AND `kill -9` (worker crash reproduction recipe)
    - File contains the literal string `MPI_Testsome` (mitigation option for worker crash)
    - File contains the literal string `py-spy` (debugging observation method in Risk 1)
    - File contains the literal string `max_wall_time_s=0.1` (Risk 4 test stub)
    - Plan 01 content is preserved: file still contains `## Candidate 1: CommWorldMap`, `## Candidate 2`, `## Candidate 3`, `## Candidate 4`
    - Total file size is at least 300 lines (`wc -l .plans/diagnose/mpi-evaluation.md` returns >= 300)
  </acceptance_criteria>
  <done>
File `.plans/diagnose/mpi-evaluation.md` contains the characterization table (4 candidates × 4 columns), paper sensitivity assessment (4 questions from D-07), and residual risks section (4 risks including the new worker-crash hang path with SIGKILL reproduction recipe). Plan 01 content is preserved. The Recommendation section is still to be appended in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Recommendation section with evidence-based rationale and Phase 1 Exit Checklist</name>
  <files>.plans/diagnose/mpi-evaluation.md</files>
  <read_first>
    - .plans/diagnose/mpi-evaluation.md (the complete document so far — append only)
    - .planning/phases/01-diagnose/01-CONTEXT.md (D-02: recommendation must emerge from analysis, not be predetermined; success criterion: recommendation must be "clear", not hedged)
    - .planning/phases/01-diagnose/01-RESEARCH.md (Summary section + Open Questions #2 for residual unknowns that belong to Phase 2)
  </read_first>
  <action>
APPEND (do not rewrite) a `## Recommendation` section and a `## Phase 1 Exit Checklist` section to the end of `.plans/diagnose/mpi-evaluation.md`. Use Edit or Write carefully to preserve all existing content.

The Recommendation section must contain the following elements in order:

1. Heading `## Recommendation`

2. A strong opening statement: "**Chosen approach for Phase 2: Candidate 1 — CommWorldMap (current default).**" followed by "This is an unhedged recommendation. Candidate 1 is the only approach that survives the elimination analysis below."

3. A subsection `### Elimination rationale` with four paragraphs eliminating Candidates 3, 4, and 2 and noting that Candidate 2 is retained for the scaling runner only. Each paragraph must include specific bug-history citations:
   - Candidate 3 elimination must cite Apr 5, 6, and 7 entries as multiple independent reproductions; mention that multiple rounds of fixes (bounding client_max_jobs, single-owner shutdown, per-call teardown hardening) did not resolve the teardown hang; note it is retained as opt-in only with the warning at `pyabc_sampler.py:296-302`.
   - Candidate 4 elimination must cite the Apr 7 "restore mapping as default" entry (worked for first abc.run() but hung on subsequent at 48 ranks) and the Apr 8 entry (single-cycle hangs in lotka_volterra). State: "pyABC's documentation assumes an MPI implementation where Disconnect is not fragile. ParaStation MPI violates that assumption."
   - Candidate 2 partial elimination (retained only for scaling runner) must include three numbered points: (1) only works in a specific pattern — one shared executor per n_workers value with MPI workloads grouped inside the `with` block — which does NOT fit the non-scaling experiment runner calling many independent abc.run() instances; (2) even in the scaling runner's happy path, the single Create_intercomm/Disconnect cycle still uses inter-communicators — the same primitive that hangs single-cycle in non-scaling experiments (Apr 8 lotka_volterra); it "happens to work" for scaling because the server-loop pattern differs, not because Disconnect is reliable; (3) requires the runner to bypass allgather coordination at scaling_runner.py:964-990, creating a second code path easy to break.
   - Closing paragraph: Candidate 2 is NOT removed — the scaling runner still uses it and will continue to work for scaling experiments. Recommendation is that Candidate 1 is preferred for NEW code and the non-scaling path. Phase 2 may optionally migrate the scaling runner to CommWorldMap as well if Risk 2 (48-rank verification) clears.

4. A subsection `### Why Candidate 1 wins` with exactly five numbered bullet points:
   (1) "It is the only candidate that eliminates the inter-communicator hang class by design." Explanation: CommWorldMap uses only bcast/send/recv on COMM_WORLD; does not call Create_intercomm, does not call Disconnect, does not rely on MPIPoolExecutor. The documented ParaStation MPI failure modes cannot apply because the primitives are absent.
   (2) "It has already resolved real bugs in production." Explanation: the Apr 8 CommWorldMap entry documents that it fixed hangs in lotka_volterra, straggler, gaussian_mean, gandk, and sbc non-scaling jobs. These are the same jobs that Candidate 4 could not run reliably.
   (3) "It is correctness-equivalent to pyABC's documented usage." Explanation: MappingSampler(map_=cmap.map) is exactly what pyABC's MappingSampler API expects: a map(fn, iter) callable. CommWorldMap.map preserves input order and propagates errors. ABC-SMC particle acceptance semantics identical to Candidate 4.
   (4) "It has negligible overhead." Explanation: no inter-communicator teardown cost; sub-millisecond per-batch coordination.
   (5) "It has clear, isolated failure modes." Explanation: the two residual risks (worker crash during bcast; 48-rank verification) are well-defined and each has a clear test recipe for Phase 2. Compare to Candidate 2's diffuse "works for this pattern but not that pattern" fragility.

5. A subsection `### Residual work for Phase 2 (MPI-01)` listing exactly four numbered tasks that Phase 2 must do:
   (1) Verify CommWorldMap at 48 ranks on the cluster (Risk 2). Note: until done, we are betting bcast/send/recv on COMM_WORLD are stable at scale on ParaStation MPI; the bet is reasonable since these are the most fundamental MPI primitives, but unverified.
   (2) Decide on worker crash handling (Risk 1). Options: MPI_Testsome with timeout (complex); rely on Slurm job timeout (simple, accepts that a worker crash means the job is lost); add wrapper to detect worker exit via OS signals (platform-dependent).
   (3) Decide whether the scaling runner should migrate from Candidate 2 to Candidate 1. Optional — Candidate 2 currently works for scaling — but unifying on Candidate 1 would eliminate the dual-code-path risk from Risk 3.
   (4) Regression tests for existing fixes: NaN weight guard (Risk 4), Barrier placement (Risk 3), try/finally on root exception path (Apr 8 fix). These protect against future regressions.

6. A subsection `### Clear verdict` with exactly four bullet points:
   - "**Adopt:** CommWorldMap as the default MPI sampler for ALL pyABC-backed inference methods."
   - "**Retain for scaling:** Shared MPICommExecutor (Candidate 2) in the scaling runner until Phase 2 clears 48-rank verification for CommWorldMap."
   - "**Keep opt-in with warning:** ConcurrentFutureSampler (Candidate 3) for diagnostic comparison."
   - "**Reject:** MappingSampler + per-call MPICommExecutor.map (Candidate 4) — no production use case."

Then append a horizontal rule and the `## Phase 1 Exit Checklist` section with the following checklist (use exactly these lines):
- `[x] All four candidates have a full coordination point inventory with file:line citations (Plan 01)`
- `[x] Characterization table populated with stability, correctness, paper effect, and residual risk per candidate (Plan 02 Task 1)`
- `[x] Paper sensitivity assessment addresses all four D-07 questions (Plan 02 Task 1)`
- `[x] Four residual risks and newly discovered hang paths documented with reproduction recipes (Plan 02 Task 1)`
- `[x] Clear, unhedged recommendation with evidence-based rationale (Plan 02 Task 2)`
- `[x] Residual work handed off to Phase 2 with specific test targets`
- `[ ] Human-verified (Plan 02 Task 3)`

End the document with a final italicized line: `*Phase 1 Diagnose deliverable — ready for human verification and Phase 2 planning.*`

Do not modify or reorder prior sections.

Per CONTEXT.md D-02 (recommendation must emerge from analysis) and the phase success criterion "clear recommendation with rationale".
  </action>
  <verify>
    <automated>grep -q "^## Recommendation$" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "Candidate 1 — CommWorldMap" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "unhedged" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "### Elimination rationale" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "### Why Candidate 1 wins" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "### Residual work for Phase 2" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "### Clear verdict" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "## Phase 1 Exit Checklist" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "Adopt:" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "Reject:" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "Retain for scaling:" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "Keep opt-in with warning:" .plans/diagnose/mpi-evaluation.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains exact heading `## Recommendation` (line-start anchored)
    - File contains the literal string `Candidate 1 — CommWorldMap` (note em-dash) in the Recommendation section
    - File contains the literal string `unhedged`
    - File contains the four subheadings `### Elimination rationale`, `### Why Candidate 1 wins`, `### Residual work for Phase 2 (MPI-01)`, `### Clear verdict`
    - "Why Candidate 1 wins" section has at least 5 numbered items (grep for `^[0-9]\\. ` in its content)
    - "Clear verdict" subsection contains all four labels: `**Adopt:**`, `**Retain for scaling:**`, `**Keep opt-in with warning:**`, `**Reject:**`
    - File contains the literal string `Apr 5`, `Apr 7`, `Apr 8` in the Recommendation section (citations in elimination rationale)
    - File contains the heading `## Phase 1 Exit Checklist` at the top level (starts with `## `)
    - Phase 1 Exit Checklist contains exactly 7 checklist items: 6 marked `[x]` and 1 marked `[ ]` (Human-verified)
    - File ends with the italicized string `*Phase 1 Diagnose deliverable — ready for human verification and Phase 2 planning.*`
    - Plan 01 content is preserved: file still contains `## Candidate 1: CommWorldMap`, `## Candidate 2`, `## Candidate 3`, `## Candidate 4`, `## Characterization Table`, `## Paper Sensitivity Assessment (per D-07)`, `## Residual Risks and Newly Discovered Hang Paths`
    - Total file size is at least 350 lines (`wc -l .plans/diagnose/mpi-evaluation.md` returns >= 350)
  </acceptance_criteria>
  <done>
File `.plans/diagnose/mpi-evaluation.md` contains the complete Phase 1 deliverable: per-candidate inventory (Plan 01), characterization table, paper sensitivity assessment, residual risks (Task 1), and the final Recommendation + Exit Checklist (Task 2). The recommendation is unhedged, selects Candidate 1 (CommWorldMap), and cites bug history evidence from Apr 5-8 entries. Ready for human verification in Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the completed mpi-evaluation.md deliverable</name>
  <files>.plans/diagnose/mpi-evaluation.md</files>
  <read_first>
    - .plans/diagnose/mpi-evaluation.md (the full deliverable produced by Tasks 1 and 2)
    - .planning/phases/01-diagnose/01-CONTEXT.md (D-02 recommendation must be unhedged; D-04 no code changes; D-05 static analysis only)
    - .planning/ROADMAP.md Phase 1 Success Criteria (1, 2, 3 — the exit bar)
  </read_first>
  <action>
PAUSE execution and prompt the user to verify the completed document. Present the what-built summary below and the six-check verification procedure. Wait for the user to type `approved` or describe specific issues. Do NOT modify the document in this task — if revisions are required, the resume-signal indicates a gap-closure plan should be created instead.

The user should not be asked to do anything Claude could have automated. This checkpoint exists solely because the quality of a judgment-heavy deliverable (recommendation fairness, evidence strength, reproduction recipe runnability) cannot be asserted by grep alone.
  </action>
  <verify>
    <automated>test -f .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "^## Recommendation$" .plans/diagnose/mpi-evaluation.md &amp;&amp; grep -q "^## Phase 1 Exit Checklist$" .plans/diagnose/mpi-evaluation.md</automated>
  </verify>
  <done>
User has typed `approved` (or the human-verification step has been completed by describing specific issues which trigger gap closure). The `[ ] Human-verified` line in the document's Phase 1 Exit Checklist is updated to `[x] Human-verified` as part of the approval.
  </done>
  <what-built>
The complete Phase 1 Diagnose deliverable: `.plans/diagnose/mpi-evaluation.md`. The document contains:
1. A header mapping the document to requirements MPI-02 and MPI-04
2. A Shared Rank Protocol section describing the all_ranks mode and the scaling-runner exception
3. Four complete candidate sections, each with a coordination point table (file:line citations) and a bug history evidence subsection
4. A characterization table with 4 rows (one per candidate) and 4 columns (stability, correctness, paper effect, residual risk)
5. A paper sensitivity assessment answering all four D-07 questions (particle correctness, wall-time semantics, overhead, paper conclusion sensitivity)
6. Four residual risks / newly discovered hang paths with reproduction recipes or test stubs
7. An unhedged recommendation selecting Candidate 1 (CommWorldMap) with elimination rationale for Candidates 2, 3, 4
8. A Phase 2 handoff listing four specific residual work items
9. A Phase 1 Exit Checklist

No code in `experiments/` was modified (per D-04).
  </what-built>
  <how-to-verify>
Open `.plans/diagnose/mpi-evaluation.md` in an editor and verify:

1. **Completeness check** — the document contains all of these headings in order:
   - `# MPI Sampler Evaluation: Phase 1 Diagnose Inventory`
   - `## Scope`
   - `## Shared Rank Protocol (all_ranks mode)`
   - `## Candidate 1: CommWorldMap (current default)`
   - `## Candidate 2: MappingSampler + Shared MPICommExecutor (scaling runner)`
   - `## Candidate 3: ConcurrentFutureSampler + Per-call MPICommExecutor (legacy opt-in)`
   - `## Candidate 4: MappingSampler + Per-call MPICommExecutor.map (the original "vanilla pyABC MPI" path)`
   - `## Characterization Table`
   - `## Paper Sensitivity Assessment (per D-07)`
   - `## Residual Risks and Newly Discovered Hang Paths`
   - `## Recommendation`
   - `## Phase 1 Exit Checklist`

2. **Fairness check** — read the elimination rationale in the Recommendation section. Does each elimination cite a specific bug-history entry (Apr date) as evidence? If any elimination is hand-wavey or lacks a concrete citation, the recommendation is hedged and needs revision.

3. **Recommendation clarity check** — read the "Clear verdict" subsection. All four candidates must have a clear disposition (Adopt / Retain for scaling / Keep opt-in with warning / Reject). No phrases like "probably", "might be", "unclear", or "could work".

4. **Coordination point coverage check** — spot-check Candidate 1's table. It should have at least 12 rows. Pick two rows and open the referenced source file (e.g., `experiments/async_abc/inference/pyabc_sampler.py` line 97) to verify the call exists at that location. If line numbers have drifted, run `grep -n "bcast\\|send\\|recv" experiments/async_abc/inference/pyabc_sampler.py` and correct them.

5. **Paper sensitivity check** — read Q4 (paper's primary claim). Does the answer convincingly establish that sampler choice is independent of the paper's main async-vs-sync comparison? If it reads as wishful thinking, flag it.

6. **Reproduction recipe check** — read Risk 1 (worker crash). The reproduction recipe should be runnable (conceptually) by someone in Phase 2 with access to a mpirun environment. Does it specify: the rank count, the signal to send, the expected hang location, and a way to observe the hang?

If all six checks pass, type `approved` to continue. If any check fails, describe the issue and this task will pause for a revision plan.
  </how-to-verify>
  <acceptance_criteria>
    - User has reviewed the document and typed `approved` (or provided specific revision feedback)
    - All 12 expected headings are present (see how-to-verify step 1)
    - No hedged language in the recommendation
    - File line numbers match current source (spot-checked)
  </acceptance_criteria>
  <resume-signal>Type `approved` to mark the Phase 1 deliverable complete, or describe specific issues that need revision (which will trigger a gap-closure plan).</resume-signal>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `wc -l .plans/diagnose/mpi-evaluation.md` returns >= 350 lines
2. `grep -c "^## " .plans/diagnose/mpi-evaluation.md` returns >= 10 (all top-level section headings)
3. `grep -c "^## Candidate" .plans/diagnose/mpi-evaluation.md` returns 4
4. `grep -E "Apr [4-8]" .plans/diagnose/mpi-evaluation.md | wc -l` returns >= 8 (bug history citations across inventory, characterization, and recommendation)
5. `grep -c "py:[0-9]" .plans/diagnose/mpi-evaluation.md` returns >= 25 (file:line citations)
6. Human has approved the document via the checkpoint
7. No files in `experiments/` have been modified (check with `git status experiments/`)
</verification>

<success_criteria>
Plan 02 is complete when:
- Characterization table exists with 4 rows × 4 columns and evidence-backed cell contents
- Paper sensitivity assessment answers all four D-07 questions with reasoning (not numbers)
- Four residual risks are documented, including Risk 1 (worker crash) as a NEW finding with reproduction recipe
- Recommendation is clear and unhedged: Candidate 1 chosen, rationale explicitly eliminates 2, 3, 4 with bug-history citations
- Phase 2 residual work list is specific (4 items with concrete test/verification targets)
- Phase 1 Exit Checklist is appended and all planning items are marked [x]
- User has verified the document and approved it
- Phase 1 success criteria from ROADMAP.md are met:
  1. Written inventory of every rank coordination point exists for each candidate ✓
  2. Each approach characterized for cluster stability, correctness, paper effect ✓
  3. Newly discovered hang paths documented with reproduction recipes ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-diagnose/01-02-SUMMARY.md` documenting:
- Completed sections (characterization table, paper sensitivity, residual risks, recommendation, exit checklist)
- The recommendation verdict (Candidate 1 — CommWorldMap)
- Residual work handed to Phase 2 (the 4-item list from Residual work for Phase 2)
- Any revisions made during human verification
- Confirmation that Phase 1 success criteria from ROADMAP.md are satisfied
- The final `wc -l .plans/diagnose/mpi-evaluation.md` line count for historical reference
</output>
